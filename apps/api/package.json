{
  "name": "@esg-rdt/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun src/index.ts",
    "build": "bunx tsc --pretty false --noEmit",
    "start": "bun src/index.ts",
    "lint": "bunx tsc --pretty false --noEmit",
    "typecheck": "bunx tsc --pretty false --noEmit",
    "test": "echo \"No API tests yet.\""
  },
  "dependencies": {
    "@esg-rdt/shared": "workspace:*",
    "@esg-rdt/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.5.4"
  }
}
